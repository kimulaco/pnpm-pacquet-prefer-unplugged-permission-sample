name: test

on:
  push:
  workflow_dispatch:

jobs:
  test:
    runs-on: ubuntu-latest
    container:
      image: cimg/node:24.14
    steps:
      - uses: actions/checkout@v6

      - run: sudo npm install -g pnpm@11.2.1

      - run: pnpm i
      - run: pnpm test
